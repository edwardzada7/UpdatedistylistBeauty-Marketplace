# iStylist backend — production requirements
# Curated list: only packages directly imported by server.py + helpers,
# plus their critical transitive deps. Do NOT regenerate from `pip freeze`
# in the Emergent dev pod — that pollutes the file with kitchen-sink deps.

# ---- Web framework ----
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0
python-multipart==0.0.20
email-validator==2.3.0

# ---- Validation ----
pydantic==2.12.5

# ---- HTTP clients ----
requests==2.32.5
httpx==0.28.1

# ---- Database / Auth ----
supabase==2.27.0

# ---- Config ----
python-dotenv==1.2.1

# ---- Background scheduler (booking reminders + no-show finalization) ----
APScheduler==3.10.4
pytz==2025.2
tzlocal==5.4
